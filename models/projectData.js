const projectData = {
    temperature: null,
    date: null,
    userResponse: null
};

// to avoid adding new props for the object model
Object.preventExtensions(projectData);

module.exports = projectData;